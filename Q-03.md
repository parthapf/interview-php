# Find the unique string of the array

There is an array of strings. All strings contains similar letters except one. Write a function to find it! 

Example:
```PHP
find_uniq([ 'abc', 'acb', 'bac', 'foo', 'bca', 'cab', 'cba' ]); // output = 'foo' 
find_uniq([ 'andx', 'xnda', 'ndxa', 'dxna', 'dxam', 'dnxa', 'adxn', 'anxd', 'axdn' ]); // output = 'dxam' 
```

Note: Strings may contain spaces. Spaces is not significant, only non-spaces symbols matters.
